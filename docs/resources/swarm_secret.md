---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "komodo_swarm_secret Resource - Komodo"
subcategory: "Swarms"
description: |-
  Manages a Docker Swarm secret on a Komodo swarm. Updating data rotates the secret in-place.
---

# komodo_swarm_secret (Resource)

Manages a Docker Swarm secret on a Komodo swarm. Updating `data` rotates the secret in-place.

## Example Usage

```terraform
resource "komodo_swarm_secret" "example" {
  swarm = "my-swarm"
  name  = "db-password"
  data  = "replace-with-secure-value"
}

# Optional secret driver and labels
resource "komodo_swarm_secret" "with_options" {
  swarm           = "my-swarm"
  name            = "api-token"
  data            = "replace-with-secure-token"
  driver          = ""
  template_driver = ""
  labels          = ["environment=production", "managed-by=terraform"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `data` (String, Sensitive) The secret value. Changing this rotates the secret.
- `name` (String) The name of the secret.
- `swarm` (String) The swarm name or ID.

### Optional

- `driver` (String) Optional custom secret driver.
- `labels` (List of String) Docker labels to set on the secret.
- `template_driver` (String) Optional custom template driver.

### Read-Only

- `id` (String) The resource identifier in the form `swarm:name`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import komodo_swarm_secret.example my-swarm:db-password
```
